[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = [
    "data",
    "logs",
    "venv",
]

[tool.ruff.lint]
# Start with correctness checks so the existing codebase has a stable CI floor.
select = ["E9", "F63", "F7", "F82"]
